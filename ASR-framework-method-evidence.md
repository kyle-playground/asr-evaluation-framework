# ASR framework 的真实方法压力证据

核查日期：2026-09-23。阅读本目录 `research-correction-methods.md`、`research-speaker-quality.md`、`research-new-moss.md` 后，重新打开下列一手论文、作者仓库和模型卡。未下载模型、未运行推理、未复现实验；本文件是设计证据，不是兼容性认证或性能排名。来源描述只支持对应方法；所有「设计压力」和「应记录」均为架构推断。

## 怎样使用这些证据

接入成功至少有三个层次：能提交并拿到结果；能在协议许可的信息和时间边界内运行；能输出满足所选指标要求的可观测结果。三者不能混成一个 `supported=true`。

下列每类都检查输入、状态与调用、输出、observable、限制和抽象冲突。共同的候选边界是：评测器掌握实验协议、证据授权、生命周期和计分；被测系统掌握算法、模型调用和内部表示。不是要求评测器重建每篇论文的内部流水线。可把整个系统作为一个被测单元；只有组件实验才要求暴露内部工件与调用轨迹。

## 1. 联合 SATS：MOSS-Transcribe-Diarize

**核查事实。** 官方模型卡称其一次生成转写、时间与匿名说话人，支持热词。标准输出形如 `[start][Sxx]text[end]`；speaker 编号仅在单条输入内有效。官方 API 的 `json` 与 `verbose_json` 分别提供原文与解析段。长录音需提高输出 token 预算。[官方模型卡及输出格式](https://huggingface.co/OpenMOSS-Team/MOSS-Transcribe-Diarize)

**设计压力。**

- 输入与侧信息：原始录音、语言/任务提示、热词及其来源。视频文件容器的支持不能推定为使用视觉口型，须由 adapter 声明实际消费的模态。
- 状态/调用：整体录音作为一次联合推理；不能强制拆成 `ASR → align → diarize` 三个可替换步骤。模型内部 token/KV 状态可保持不透明。
- 输出：同时有文本、时间区间、局部 speaker 引用，可有声学事件；保留原始生成串与解析后结构，区分解析失败和识别失败。
- 应记录：段边界、speaker 作用域、输出是否完整、停止原因、输入覆盖范围、生成预算；指标分别依赖文字、时间、归属，不能只有最终 WER。
- 不可伪装：模型段时间不能标为原生词时间；匿名 speaker 不能当真实身份；单次长音频和 token 逐步返回都不足以证明在线因果流式。
- 冲突：`audio → string` 会丢掉联合输出；强制给每个系统插入统一 forced aligner 会变成另一个组合系统，而不是无损格式适配。

**待查。** 当前具体服务对部分输出、非法时间、截断原因的暴露程度；联合结果对重叠的实际覆盖；不同服务版本解析一致性。模型卡能力不是这些测试的替代。

## 2. 目标说话人条件：DiCoW / SE-DiCoW

**核查事实。** DiCoW 用帧级 diarization 概率构造 STNO 条件；SE-DiCoW 为解决完全重叠时条件近同的问题，从整段录音选目标最活跃登记段，用交叉注意力条件化。它仍按 Whisper 的 30 秒窗口顺序解码。论文同时列 oracle 与预测 diarization；摘要的 52.4% 对比在正文明确属于 oracle 条件。[论文 §2、表1](https://arxiv.org/html/2601.19194v1)

**设计压力。**

- 输入与侧信息：混合音频、目标 speaker 引用、逐帧活动概率、由允许范围音频提取的登记片段。oracle 活动与模型预测活动必须是不同实验条件。
- 状态/调用：一个录音可按多个目标调用；目标 ID、时间轴与登记来源需稳定。是否共享 encoder 计算留给系统实现，成本不能按“一个音频一个请求”估计。
- 输出：每个目标的文本/时间；归属来自条件绑定，不能假设输出中必须含 speaker token。
- 应记录：目标集合、条件来源、登记片段区间、可见音频截止点、调用映射、跨目标重复/漏词和总成本。预测 diarization 成本纳入端到端系统；组件测试可以固定并明确排除。
- 不可伪装：从会话后半段取登记信息再识别前半段，属于离线可用证据；把窗口缩为 30 秒不能获得因果性。也不能把真实标签伪装成预处理元数据。
- 冲突：单输入单输出接口及固定说话人标签阶段不足；框架应支持有作用域的 target 条件，而不是为每个模型添加专用 STNO 字段到全局样本。

**待查。** 具体发行模型支持的登记长度、无可用独讲段回退、概率时间轴转换与目标数限制，应在适配实现前钉定模型/源码版本。

## 3. 流式修订与跨块状态：Whisper-Streaming / Streaming Sortformer

**核查事实。** Whisper-Streaming 的 LocalAgreement 用连续更新的一致前缀确定已确认文本，音频缓存随已确认完整句的时间滚动；仓库提供音频块到达和实时模拟说明。[作者仓库](https://github.com/ufal/whisper_streaming) Streaming Sortformer 的 AOSC 按出现顺序存先前说话人的帧级 embedding，并依据历史预测更新。[作者论文摘要](https://arxiv.org/abs/2507.18446)

**设计压力。**

- 输入与侧信息：按协议时钟到达的音频块；明确 lookahead、端点信号和到达计划。不能预先把完整录音路径交给声称受限可见范围的执行器却只靠约定不读取。
- 状态/调用：会话开始、连续 push、排空/结束、reset；假设缓存和 speaker cache 的生命周期属于会话。快照是可选能力，不是所有后端必需条件。
- 输出：假设版本、替换范围、确认范围、speaker 活动事件。两个代表分别证明文本稳定化与说话人记忆的需求，不声称它们已组成现成联合系统。
- 应记录：输入媒体时间、输入到达时间、结果被 harness 观测时间、结果版本及 supersedes 关系、确认时刻、最终状态、缓存重置事件。可据此计算用户可见延迟、修订负担、speaker 漂移及最终质量。
- 不可伪装：最终全文不能还原真实修订轨迹；不暴露 tentative 输出的后端，只能测可见的 committed 延迟；不能用模型自报时间替代接收时间，也不能把离线快跑回放当实机低延迟。
- 冲突：独立样本并行调度/任意重排会破坏缓存；只保存最终文本完全丢失在线行为。同一个系统可以参加离线 final-quality 投影，但这不授予 streaming 能力。

**待查。** 后端实际暴露哪些修订、缓存是否可恢复、最大 speaker 数、断线后语义。框架应允许“该维度不可观测”，不能编造空轨迹表示零修订。

## 4. N-best / lattice 受限纠错：N-best T5

**核查事实。** 论文将 N-best 作为 T5 输入，另有 N-best 或 lattice 受限解码，并与原 ASR 分数插值。lattice 由 Transducer path merging 产生；跨 ASR/T5 tokenizer 需经过词 lattice 转换。[论文 §2–3](https://www.isca-archive.org/interspeech_2023/ma23e_interspeech.pdf)

**设计压力。**

- 输入与侧信息：候选列表、分数、或带路径及边分数的图；生产者 checkpoint、分词器、beam/path-merging 设置构成候选身份。
- 状态/调用：可在冻结的候选工件上跑纠错器；图变换由适配器或系统内部负责，不要求框架核心理解每种 decoder 图。
- 输出：选中路径/候选或生成文本；“选整条候选”“图内重组”“自由生成”是不同约束声明。
- 应记录：原始候选 artifact、生产配置、score 语义/尺度、约束模式、最终路径引用、首轮和后处理成本。oracle WER 由评测器独立计算，不能反馈给被测算法。
- 不可伪装：API 返回多个答案不保证是同一模型的 N-best；1-best 复制 N 次不构成候选能力；token 分数不必是纯声学似然；图不能无损压成小候选表。
- 冲突：如果统一适配器强制所有结果只剩文本，后续组合无法接入。反过来，为所有后端要求 lattice 会排除正常黑盒系统。应对工件声明格式/语义和消费兼容性，按条件拒绝不支持组合。

**待查。** 公共具体模型/API 是否真正能导出所需图及分数；tokenizer 转换是否保留路径和 score。论文机制并不保证任意黑盒后端能外挂。

## 5. 纠错器回调 ASR：ECLM correction-first decoding

**核查事实。** 原 ASR 先 greedy；ECLM 从该文本生成候选；原 ASR 再计算这些新候选的分数，与 ECLM 分数融合。论文另有无需声学分数的纯文本 greedy 纠错，不能混为同一机制。[论文 §3.3 / Algorithm 1](https://arxiv.org/html/2405.15216v2)

**设计压力。**

- 输入与侧信息：首轮文本、原音或原声学特征、给指定文本评分的 ASR 服务；服务引用需绑定同一模型版本和音频。
- 状态/调用：数据依赖形成 `识别 → 候选生成 → 指定候选评分 → 选择`；ASR 作为识别器和 scorer 被两次使用，不能仅以一次完成的 stage 描述能力。
- 输出：最终文本及可选候选分项 score。
- 应记录：新候选来源、声学评分调用数/批量、score 定义、有效音频范围、总推理成本、无效候选及评分失败处理。
- 不可伪装：不能用原 1-best confidence 冒充任意候选似然；forced alignment 成功也不等于候选有高识别概率。只提供转写 API 的后端应判定这个组件组合不兼容。
- 冲突：固定“前处理/ASR/后处理”单向阶段把回调藏起来会误计成本并错误缓存。可以把此方法整体封装为系统；组件级组合才需要显式的 candidate-scoring 服务契约。无需为此把框架变成支持任意循环的工作流引擎。

**待查。** 各 CTC、seq2seq、transducer 具体后端的指定文本评分成本、近似、长度归一化。不同模型的 raw score 不可默认互比。

## 6. 领域检索与可变会话记忆

**核查事实。** Amazon 的工作用初次 ASR 文本检索上下文，供文本或音频 LLM 纠错。[作者研究页](https://www.amazon.science/publications/contextual-asr-with-retrieval-augmented-large-language-model) Ontology Memory 方法在会话内维护实体和别名，以因果历史纠错，再用输出更新记忆；文本段视为可靠输入。RAMC-Corr 测试为 43 会话。[论文 §2–3 / 表1](https://arxiv.org/html/2606.13464v1)

**设计压力。**

- 输入与侧信息：静态检索库、可靠历史文字、当前首轮转写、此前系统输出；它们分别是外部资源、协议输入与算法派生状态，不能统称 `context` 而失去权限和时间来源。
- 状态/调用：按会话顺序检索、纠错、更新；会话切换要 reset。随机并行 utterance 不再等价。外部库和动态记忆是两种生命周期。
- 输出：修订文本，以及在可观测组件实验中保存的检索条目引用、记忆版本/更新事件。
- 应记录：库快照/索引版本、条目来源和可用时间、初始记忆、读取及更新摘要、上下文预算、失败回退、检索与建库成本。比较冻结记忆和自反馈记忆时分别命名实验条件。
- 不可伪装：协议提供的可靠历史是额外监督资源；未来文本、当前参考答案、跨测试会话残留不可当作普通检索资料。论文中的“在线”是因果会话处理，不自动等于音频低延迟流式。
- 冲突：`cache(audio_hash, model)` 无法标识此方法结果；至少要考虑协议、资源版本和前置状态。由框架核心实现某篇论文的 ontology schema 会过拟合；框架只需验证资源权限、顺序及隔离，记忆组织属于算法。

**待查。** 真实部署中可靠文字比例、历史纠错误差的积累、条目过滤是否可审计。43 段会话上的机制证据不能证明跨领域鲁棒性。

## 7. 视听双候选：DualHyp + RelPrompt

**核查事实。** DualHyp 让 LLM 整合独立 ASR 与 VSR 的 N-best；RelPrompt 提供各模态随时间的可靠性线索；作者仓库明确列出 reliability predictors，并发布来自 Whisper-large-v3 与 BRAVEn-large 的候选。实验在 LRS2 多种损坏情形下评测。[ACL 论文页](https://aclanthology.org/2026.findings-acl.26/)、[作者仓库](https://github.com/sungnyun/dualhyp)

**设计压力。**

- 输入与侧信息：音频、同步口型视频，或已冻结的音频/视觉候选及可靠性提示。若只评纠错模块，必须明确候选生产过程不在运行范围；端到端则要计入 VSR 等成本。
- 状态/调用：两个分支独立出候选后融合；同步、帧丢失和模态缺失规则属于协议/适配契约。
- 输出：融合后的转写，可附分支候选引用；不能要求它天然产生 speaker 或时间标注。
- 应记录：消费模态、媒体时间轴、分支模型版本、可靠性信号来源、噪声生成种子/强度、前端失败、分支与融合成本。
- 不可伪装：需要口型信息的结果不能和仅音频结果不加说明地排同一榜；可靠性提示若来自已知人工 corruption 标签，必须声明额外信息，不能当作部署时已可预测。
- 冲突：把 `audio_uri` 改名 `media_uri` 不够，多模态资源及派生工件需有各自来源和同步关系。框架不应把“任何视频容器输入”当为 visual capability。

**待查。** 仓库已证实存在可靠性预测器；但各实验是否使用同样的预测器、训练时 corruption 监督与推理时输入边界，仍需逐配置核查，不能把所有配置判成 oracle 或全自动。当前核查足以确立多模态证据需求，不足以认证现场视频端到端协议。

## 8. 多通道、分离与系统融合：NTT CHiME-8

**核查事实。** 系统组合 diarization、GSS、ASR 和候选融合；使用多麦克风 speaker counting，前端进行麦克风选择、去混响和多通道滤波。论文有固定 baseline ASR 与 oracle diarization 的组件实验，不能当为整个部署系统效果。[论文 §2–3、§6](https://arxiv.org/html/2409.05554v1)

**设计压力。**

- 输入与侧信息：同步多麦克风录音与通道标识；预测活动或诊断 oracle 活动。不能在统一 ingest 层不可逆降为 mono。
- 状态/调用：一个 session 派生多个通道组、目标流与分段，ASR 后再合并；产生的音频段必须带原录音时间映射。
- 输出：按 speaker/time 组织的多流识别及融合结果；中间波形和候选仅在需要组件分析时公开。
- 应记录：实际使用通道/选择规则、派生音频父级、时间偏移、speaker/流对应、重叠区重复与漏词、所有分支成本。
- 不可伪装：不把增强波形当新的独立测试样本，不按拆流后的更长总时长稀释实时因子；不能把分离变清晰直接当文本改善。
- 冲突：一条样本一段 mono 音频会丢失空间证据；一个线性 stage 链不表示分支。整体黑盒评测只需保留 session 身份、资源边界和最终结果；细粒度工件关系按分析需求启用。

**待查。** 接入的数据是否提供实际所需同步、多麦信息；独立复现的组件版本、资源预算及缺失通道回退。

## 跨方法的架构判据

以下均为设计建议，不是来源论文提出的统一 framework。

| 被反例击穿的假设 | 保持稳定的边界 | 不应加入核心的内容 |
|---|---|---|
| 一个模型就是音频到字符串 | 被测系统 + 结果声明 + 明确的评分投影 | 为每个模型造一条专用执行路径 |
| 所有音频块独立 | 会话生命周期、时间可见范围、状态作用域 | 对所有状态要求可序列化张量 |
| 后处理只能消费文本 | 有语义和来源的可选工件/服务能力 | 通用化每一种 lattice 或 embedding 内部结构 |
| 相同数据就是公平比较 | 相同协议下的资源、侧信息和可用时间 | 将 oracle 和预测结果混排 |
| 输出时间戳等于实时性 | 媒体时间与实际观测时间分离 | 从最终文本推造过程轨迹 |
| 任何系统都能算所有指标 | 每个指标检查所需输出粒度/参考条件 | 静默 forced alignment、静默补 speaker |
| 缓存只需要音频和模型 | 协议、配置、资源、输入工件和状态共同定身份 | 跨 session 复用未经声明的记忆 |

建议用这些案例做纸面接入审查：先问“整体系统能否受控运行并合法评分”，再问“组件实验需要多深观察”。不要求所有方法开放内部图、score 或记忆；不透明实现仍可做边界清楚的端到端比较，但不宣称完成内部归因。缺失所需证据时标明不支持对应 profile/metric，比自动造出能力更可靠。

## 最有破坏性的五个反例与仍未解决的事实

1. SE-DiCoW 在前段识别中可用后段登记：迫使协议约束证据时间，而不是依据分块大小声明 streaming。
2. ECLM 把 ASR 作为后续候选 scorer：迫使识别和评分能力分离，或以整个算法封装；不能仅靠后处理 string callback。
3. 会话记忆由自身结果更新：迫使缓存、调度、复现考虑因果历史，不再是样本独立函数。
4. 联合 SATS 不存在独立对齐/diarization 模块：迫使统一结果与统一内部算法分离。
5. lattice、视觉候选、多通道都是不同证据：一个 `extras` 字典可以装下它们，却不能验证权限、兼容性或可比性。

还未验证的事实集中在具体后端：真实流式事件可见程度、模型原生时间粒度、候选 score 可导出性、指定文本评分语义、多模态可靠性来源、运行失败与恢复行为。以上应作为接入核查项；本轮没有把未知项用架构假设补成“已支持”。
