# ASR framework 方法适配与反例压力测试

2026-09-23。这是对[总体设计](ASR-framework-design.md)及[数据设计](ASR-framework-data-design.md)的纸面适配检查。没有执行模型、SDK 或性能测试。“无需改核心”指当前设计能表达，不是已实现 adapter。

## 1. 判定方法

每遇到一种方法，依次回答六个问题：允许读取什么；状态持续多久；怎样调用；产生什么；能合法评哪些指标；新接入到底改哪里。仅仅能把结果塞进 `extras` 不算通过。

变化级别：A＝新 adapter/参数；R＝新组合方法或其内部能力；D＝数据 view/协议配置；S＝评分 adapter/扩展类型；K＝改变核心语义。A/R/D/S 是正常扩展，K 需要解释为什么现有语义不成立。一个方法可以需要多个扩展级别。

证据深度区分：E＝本轮重新核查一手来源，见[证据记录](ASR-framework-method-evidence.md)；L＝基于目录已有一手资料调研，未为本设计重新逐版本核查；H＝假设性未来方法，仅用于设计测试。

## 2. 代表方法矩阵

| # | 代表/机制与证据 | 输入及状态 | 输出与合法评测 | 扩展位置 | 禁止的“兼容”方式 |
|---|---|---|---|---|---|
| 1 | Whisper / faster-whisper 整段转写，L | 音频、冻结语言/提示；case 内窗口 | Transcript，原生可用时间；文字/长录音质量 | A | 把滑窗自动叫实时；偷偷统一 beam 含义 |
| 2 | CTC + 外部 LM，L | 音频、LM/lexicon；内部帧分布 | 文本，可选 CandidateSet；文字/候选 oracle | A/R | 要求所有模型导出同型 logits；把 LM 资源成本隐藏 |
| 3 | RNNT/TDT/cache-aware streaming，L | 有序块、look-ahead、会话缓存 | revisions/提交；实时质量和延迟覆盖 | A/D | 依据训练损失名称判断流式；独立并行每块 |
| 4 | Paraformer 在线/离线权重，L | 与所选发行版本匹配的模式与状态 | 对应模式下的文字/事件 | A | 将离线权重套一个 push wrapper 就标原生在线 |
| 5 | SenseVoice 富标签输出，L | 音频；语言/任务设置 | Transcript + 事件扩展；文字和有对应 gold 的事件指标 | A/S | 把情绪/声音控制标签混进 CER 或无版本地全删 |
| 6 | 文件转写云 API，L | 授权音频、有限配置；远程 job | 最终输出、错误、客户端用量；文件交付质量 | A | 未提供词/候选也补出来；将轮询时间称 GPU 时间 |
| 7 | 流式云 API，E（AWS 文档） | 增量音频、连接状态 | partial、stable、segment final；可见维度延迟 | A/D | 追加所有 partial，或把 final 当整场不可修订 |
| 8 | Audio LLM 提示转写，L | 音频和 task prompt | 解析为转写 view，原响应保留；文字质量/格式失败率 | A | 用 QA 正确率替代逐字稿；用 gold 帮忙解析 |
| 9 | VAD + chunking + ASR，L | 原音；分块上下文 | 派生音频、时间映射、合并稿 | R | 删除静音后丢原时钟；重复窗口分别计作新录音 |
| 10 | 降噪/增强 + ASR，L | 原音、增强设置 | AudioView 与端到端稿 | R/D | 模型专属增强藏在 dataset loader；只测增强后 SNR |
| 11 | WhisperX/独立对齐 + pyannote，L | 音频、转写、预测活动 | Alignment + Attribution；文字/时间/归属分开 | R/S | 对齐证明文字正确；exclusive 活动当重叠 gold |
| 12 | MOSS 联合 SATS，E | 完整录音、提示；联合解码 | 文字、段时间、局部 speaker；满足粒度时用多说话人指标 | A | 强行拆三个步骤，伪造词时间、独立阶段耗时 |
| 13 | DiarizationLM 标签纠正，L | 固定词序与说话人预测 | 新 Attribution；归属指标与文字不变量 | R | 文本也改了却声称是纯标签消融 |
| 14 | N-best T5 / lattice 受限纠错，E | 候选图/分数/分词语义 | 新 Transcript/路径来源；文字与候选覆盖 | R/S | 黑盒 1-best 复制成 N-best；图丢路径仍叫无损转换 |
| 15 | ECLM correction-first，E | 音频、首遍稿、给新候选评分的 ASR | 新稿、候选分数、调用用量 | R | 用 1-best confidence 冒充任意候选声学分数 |
| 16 | ROVER/MBR 多系统融合，L | 多个不同/采样候选与明确权重 | 融合稿与父候选；错误、总计算/成本 | R | 投票共识当正确概率；总耗时用最快一路替代 |
| 17 | 置信门控局部二遍，L | 原音、首遍预测、可校准风险信号 | 决策、局部重识别、修订稿 | R | 所有模型 0.8 当相同概率；局部改字沿用旧 alignment |
| 18 | RAG/ontology 会话记忆，E | 库快照、允许历史、因果状态 | 修订稿、可观察记忆版本 | R/D | shuffle turns；只用音频和模型做缓存；跨会话残留 |
| 19 | DiCoW/SE-DiCoW，E | 音频、目标活动、登记片段 | 目标条件文字；多目标聚合的 cp/tcp | A/R/D | 将 oracle 活动当元数据；后段登记用于前段却叫因果在线 |
| 20 | NTT CHiME-8/GSS/多麦融合，E | 同步多轨、活动、分支处理 | 多流音频/文字、speaker 与时间 | R/D | ingest 丢空间信息；用拆分后总时长稀释 RTF |
| 21 | Whisper-Streaming / Sortformer，E | 音频流、历史确认/说话人缓存 | 文字或活动 revisions | A/R/D | 从最终稿补造事件；视两种研究为现成集成系统 |
| 22 | DualHyp 视听融合，E | 同步视频与音频候选、可靠性来源；区分推理预测与人工 corruption 信息 | 融合文本；视听辅助独立条件 | R/D/S | 视频容器当视觉推理；额外视觉信息混入音频榜；未经逐配置核查将 RelPrompt 一概认作 oracle |
| 23 | ITN/双形式 ASR，L | 口语稿及上下文/原音 | spoken/display/value 与映射；对应层指标 | A/R/S | 书面稿冒充忠实逐字；数字归一化消掉实质错误 |
| 24 | 在线自适应/测试时学习，H | 因果序列、允许反馈、适应状态 | 预测及更新状态摘要；时间顺序实验 | A/R/D | 测试 gold 驱动更新却叫无监督；测试顺序不入身份 |

结论：以上方法在当前设计中均能定位到 A/R/D/S，不要求为模型名增加核心分支。但不少组合会被合法拒绝；这也是通过设计检查的一部分。框架没有承诺任意后端支持任意 recipe。

## 3. 七个深入推演

### 3.1 MOSS 与 WhisperX + diarization 怎样公平比较

共同输入：相同完整录音、语言/热词允许规则；不额外提供真实分段或人数。MOSS 作为一个 whole-case adapter；级联系统作为一个组合方法，两者都输出相同语义的结果束。

文字、归属、时间产物可以源于同一个联合输出，也可以来自不同父步骤。MOSS 的段时间保留段级，级联系统的词时间保留词级；不为了表面一致偷偷给 MOSS 增加对齐器。

若选择允许段级近似的时间约束评分，就冻结同一近似规则并清楚标为该协议；若要求真实词级时间而某方法无此能力，则该方法不能参与该项，或另建“MOSS + aligner”方法后计入全部代价。共同的文字/归属任务仍可评。

需要观察：完整性/截断、原始输出解析、匿名 speaker scope、端到端成本。不能从“模型一次联合输出”推定其活动时间线与外部 DER 活动定义等价。

**架构结论**：统一输出任务，不统一内部步骤；变化局限 A/R/S。联合输出是阻止过度组件化的重要反例。

### 3.2 ECLM 与普通 LLM 文本纠错不是同一个端口需求

输入资源是相同冻结首遍文本。纯文本 LLM 只需 Transcript；ECLM correction-first 还需要原音与可为指定新候选评分的后端。

组合流程：首遍 artifact → 候选生成 → 调用 ASR candidate-scoring 能力 → 选择输出。candidate-scoring 是方法内部的可选服务 Seam，绑定具体音频、checkpoint、tokenizer、分数/长度归一化语义与计量，不是全框架强制方法。

如果用户选择的云 API 没有此能力，预检报告“该 recipe 缺少 candidate-scoring”，不退化为普通文本纠错后还沿用 ECLM 名字。也可以把已经实现完整算法的外部系统整体接入，从而不暴露内部 scorer；此时只能做端到端比较。

同预算对照：不纠错、纯文本纠错、声学重打分完整方案；声学后端重复访问音频的代价全部计入。端到端质量可以比较，算法内部收益分解需更多可观测步骤。

**架构结论**：输出 artifact 与主动能力调用不同；不能把一个函数调用伪装成候选文件。但这只需要 recipe 内的能力契约，不需要通用循环引擎或修改主 runner。

### 3.3 会话记忆必须同时改变调度和数据视图

会话 turn 1…N 形成一个 Episode，输入资源包含允许的可靠历史或空初始状态。第 t 轮仅访问协议允许的既往状态；方法读取检索库快照并产出转写后更新自己的记忆。

冻结的纠错输入还要保留首遍来源。缓存 key 除当前文本外包含前置状态身份；第 2 轮改了，会影响后继实际状态，不能只重跑第 2 轮后复用旧第 3 轮。

两套对照分别是固定历史记忆和基于模型输出持续更新记忆，不能让某方法用人工真历史而另一个用自身错稿。若业务确实有人确认历史，可以单列 human-assisted 协议。

允许跨 episode 并行；episode 内保证顺序。重试若不能恢复精确状态，从可验证 checkpoint 或起点重放，保留额外成本。gold 只在评分路径。

**架构结论**：需要 D/R，而不是为该论文新增全局 OntologyMemoryManager。数据负责先后/来源；运行负责有序调度；记忆组织仍是方法 Implementation。

### 3.4 流式一次“您好→你好→你好啊”怎样记录

手工契约样例，不是实测：参考为“你好啊”，参考结束时刻 1.50 s；同一 scope 的三个观察 snapshot 分别为 0.90 s“您好”、1.30 s“你好”、1.80 s“你好啊”。另有 2.00 s 的文字提交声明，2.40 s 只修改 speaker 标签。

回放显示先后替换，不生成“您好你好你好啊”。最终文本 CER 为 0，但修订负担非零。若参考词/字时间完备，按固定 trace 对齐计算各正确 token 稳定时刻；否则只计算句级稳定时间。1.80 s 后文字没再改，因此事后稳定与 2.00 s 的提交声明不同。

2.40 s 改归属不算文字回滚，但会改变 speaker 维度稳定时间。若 2.00 s 声明文字不可改后又改字，记录承诺违约。若到截止还未成功输出“啊”，不能只统计“你好”的快延迟并遗漏“啊”。

**架构结论**：需要按维度提交、scope revision 和覆盖率；一个 `is_final` 不够。事件压缩方式不是公共语义，可留在存储内部。

### 3.5 SE-DiCoW 的 30 秒窗口不等于 30 秒可见信息

一手论文表明其自登记可从整场录音选目标片段。因此正常离线录音任务可以提供完整音频；如果输入端规定实时因果，只能在当时已到达音频中选登记，原论文流程不能直接原样通过。

不需要新增 SE-DiCoW 特例：InputView 的 availability 约束会普遍挡住“前段识别用后段证据”。修改为因果登记的实现会成为新的 MethodSpec，必须重新评价，而非由框架静默替换。

另一维度是活动来源。预测 diarization 是系统步骤或冻结预测资源；oracle diarization 是独立辅助条件。原有 [speaker 研究文件](research-speaker-quality.md)引用的 52.4% 相对收益需按本轮证据补充 **oracle diarization 条件**，本设计不据此设定实际系统预期收益。

**架构结论**：信息可见时刻属于协议/数据语义；时间戳或分块大小不能替代输入访问约束。

### 3.6 多麦分离为何不能归约成一个音频文件列表

输入是一场录音的同步多轨。通道选择/分离是方法决策；产物包含若干分离 AudioViews，每个保留共同原时间线与来源，流 ID 并不承诺真人身份。

多路 ASR 产生文本后，方法决定 speaker 关联和融合。若任务只要求可交换输出流，采用相应 ORC/MIMO 类型协议；若要求持续 speaker 身份，采用 cp/tcp，不能把两种任务混为一种。

代价报告以原录音时长作为用户音频基准，同时额外显示通道时长/提交时长、累计资源。不要把 1 小时会议拆成 4 路后，用 4 小时作为主 RTF 分母让系统看起来更快。

**架构结论**：D/R 足够，核心需要支持多资产/时间映射；不需要为每个 GSS 参数建字段。

### 3.7 只改 speaker、只改 ITN、只改评分怎样判定影响

三个不同变更：

1. 标签纠正只生成新 Attribution，Transcript ID 不变；使用同一固定词序的普通文字分数必须不变。若普通分数变了，先检查 scorer 是否按新 speaker 排了序。
2. ITN 产生新的 display view 和跨度映射；spoken view 保留。是否改善规范化文字分数和实体值，需要对应 ScoreSpec，不能凭“更像书面语”证明识别更准。
3. 仅调整评分 normalizer，方法结果完全不变；产生新的 ScoreRun，不能把这次变化归因于模型。

**架构结论**：版本化的语义视图让影响局部化。一个可反复覆盖的 `text` 字段无法支持这三种实验。

## 4. 应明确拒绝的组合

| 请求 | 预期拒绝/降级结果 |
|---|---|
| 只有文件最终文本，要求首 token/稳定延迟 | 缺实时 trace；可以另跑真实观察实验，不能事后生成 |
| 只有段时间，要求原生词边界误差 | 粒度不满足；外接 aligner 后成为新方法，或独立近似协议 |
| 只有 1-best 的 API 加 lattice constraint | 缺候选图能力，不允许造假 |
| 无字级/词级 gold 的数据测词时间误差 | 数据资格不满足，独立评句级或补标注 |
| 未标注片段视为空参考 | 数据语义错误，拒绝；确认非语音才允许静音评分 |
| 两份结果 reference/normalizer 不同，排同一榜 | ComparisonKey 不兼容；可重评分后再比较 |
| 恢复流式状态但后端不支持 checkpoint | 新 attempt 重放/终止，不承诺无缝续传 |
| 把人工参考构建的热词称为零辅助输入 | 改列 oracle 条件或移除该输入后重跑 |
| 只有某一人的输出，要求全会议 speaker 转写 | 任务输出不完整；目标说话人任务可另列 |
| 新方法只输出摘要或 speech-to-speech 音频 | 无逐字稿任务输出；可通过附加转写构成新系统，不能当原模型 ASR 分数 |

## 5. 真正会推动核心演进的未来情形

以下不假装已经无成本支持：

- **主动交互识别**：方法要求用户重说、选择候选或提供新录音。若要测实际交互，应新增交互环境 profile 和反馈成本/任务完成指标；静态录音回放没有真实用户响应，不能用 gold 回答方法的问题。
- **设备闭环信号处理**：方法能改变麦克风采样、波束方向或录音设备行为。单纯既定媒体供给不能覆盖，需设备环境及同步观察契约。
- **无显式转写的语音代理**：ASR 框架可评价额外审计转写通路，但完整动作/对话成功需要独立任务，而非扩大 WER 含义。

这些涉及“被测对象能改变未来输入或任务目标”，与新增一种解码器有本质不同。核心允许通过新 profile 正式演进；不为了宣称万能，提前把所有功能装进无类型回调。

## 6. 设计结论的证据范围

当前 24 类方法/机制的检查覆盖：基础识别、云端/本地、联合输出、组合流程、主动回调、条件路由、多候选、状态记忆、多通道、多模态、流式修订和双形式文本。

其中 8 类有本轮一手方法证据，其余依赖已有调研或明确是假设。完成的是契约层覆盖和拒绝规则；未证明第三方 SDK 行为符合声明、模型能在本机运行、指标实现完全正确或性能可接受。这些由[架构审查的验收计划](ASR-framework-architecture-review.md)承担后续验证。
