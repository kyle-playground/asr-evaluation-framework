# ASR framework 架构审查、设计取舍与验收计划

2026-09-23。审查对象为[总体设计](ASR-framework-design.md)、[数据抽象](ASR-framework-data-design.md)、[方法压力测试](ASR-framework-method-fit.md)。所有“通过”仅指设计表达一致；尚未编写或执行实现与测试。

## 1. 从架构角度先问什么

最有价值的设计步骤不是继续增加类名，而是把易变化的事实与必须稳定的语义分开。本次采用以下顺序：

1. 从现有调研提取变化轴：输入信息、状态、时间、输出结构、算法控制流、评分需求。
2. 选反例而不是堆模型名：联合模型、主动回调 ASR、会话记忆、多麦/视觉、修订流式。
3. 确定外部可观察的契约，再讨论方法内部怎样组合；不要求联合模型被切开。
4. 将数据也做来源/资产/标注/视图/供给抽象，分离可见输入与评分参考。
5. 给常见变更写“改动应该停在哪里”，用 locality 检查接口是否过宽。
6. 用失败、缺能力、标注不全、缓存失效等反向样例审查；只画成功路径会掩盖关键缺陷。
7. 以可手工推演的契约样例定义实现验收，留下仍须运行验证的假设。

每一步都降低一种具体不确定性。单纯多阅读十个类似音频→文本模型，对接口价值小于认真检查一个会话记忆方法的状态生命周期。

## 2. 三个候选架构与选择

以下是本次设计中的备选取舍，不是已经实现的三个原型。

| 方案 | 好处 | 会失败的具体地方 | 判断 |
|---|---|---|---|
| 固定流水线：AudioPreprocess→ASR→TextPostprocess | 容易开始，常见 VAD/纠错链直观 | 联合 SATS 没有独立三阶段；ECLM 回调 ASR；多轨分支和有状态流程只能塞专用 hook | 不作为核心约束，可当一种 recipe |
| 通用节点图/事件总线：一切都是 Node | 任意连接，表面上都能表示 | 每个节点都要学习调度、状态、重试、类型和时间；“能画成图”不保证可比、可缓存、无泄漏 | 初版不建通用工作流引擎 |
| 协议驱动的完整方法 + 产物/观察 + 可选组合 | 联合/黑盒和可拆方法共存；评测语义集中；接入局部 | 组件诊断需要选择性暴露；whole-case 与 incremental 不能强求一模一样接口 | 采用；以显式执行 profile 保留真实差异 |

不是把三套方案全部叠起来。保留的只有第三种外部契约；线性链或小图都是方法内部实现选择。现有 harness 也只能接在明确 Seam 上，不能再创造第二套隐式的评分与失败规则。

## 3. 四轮反例审查与设计修正

### 第一轮：统一结果而不是统一算法

**初始诱惑**：把模型、VAD、分人、对齐都设计为平级 Stage。

**反例**：MOSS 一次联合生成文字、段时间和 speaker；普通云 API 只给最终文本。强行平级 Stage 会伪造独立输出/代价，或者为每个例外增加绕行逻辑。

**修正**：完整方法统一对外结果；不同语义结果分为 Transcript、Alignment、Attribution 等，可共享同一 producer。内部中间产物按需公开。

**保留的限制**：联合模型不能直接进行“只换内部 diarization”的受控消融。框架不能制造方法本来没有的可分解性。

### 第二轮：区分产物与主动能力

**初始诱惑**：后处理通过消耗首遍 Transcript/CandidateSet 就足够表达。

**反例**：ECLM 生成新候选之后请求原 ASR 评分；lattice 解码需要图而不只是候选字符串；局部二遍需要回取原音。

**修正**：方法内可有主动调用 Seam；公共 artifact 只描述保存下来的证据。将专用 candidate-scoring 契约留在组合方法内部，不强制所有 ASR 提供它。

**保留的限制**：黑盒后端不能接这个 recipe 就明确拒绝。全系统 adapter 仍能评最终结果，但不能宣称已经完成内部归因。

### 第三轮：状态、时间与输入可见性进入评测契约

**初始诱惑**：样本可以任意并行；最终文本和一个 `is_final` 足够；30 秒分块即表示流式。

**反例**：ontology 记忆依赖过去输出；Sortformer 维护 speaker cache；SE-DiCoW 登记可来自后文；词稳定不等于段 final；说话人标签可以在文字定稿之后修改。

**修正**：增加显式 episode/state scope；区分 whole-case 与 incremental 生命周期；输入资源有 availability；结果按 view/scope/revision 发布，提交承诺按维度记录；观察时钟与媒体时间分开。

**保留的限制**：未知隐藏状态导致无法强复现；没有过程事件只能评最终质量，不能算曾经的回改体验。

### 第四轮：数据也必须有独立深度

**初始不足**：总体设计已有 Case/InputView/ReferenceBundle，但不足以说明新数据源、派生视图和不完整标注如何接入。

**用户补充**：数据部分也能抽象。

**修正**：独立定义 Source Adapter→Dataset Snapshot→Evaluation View→Case Manifest→Protocol Projection→Delivery；媒体、标注与冻结预测分别有身份及来源；切片/变换有参考有效性规则。

**实际收益**：同一数据可以合法支持短句、长录音、会话记忆和流式；新增 HF/本地来源不触碰模型；没有时间标注的纯文本数据仍可做纠错。未标注与静音不再混为同一空串。

**保留的限制**：不能自动从整句 gold 制造任意秒级切片参考；不能将多轨/视觉未知同步用常量假设补齐。

## 4. 关键决定记录

这里只记录与本 framework 有关的取舍，不建立额外 ADR/领域建模体系。

| 决定 | 原因 | 代价/风险 | 何时重新审视 |
|---|---|---|---|
| 完整方法是主要被测对象 | 联合、黑盒和组合方法都是真实存在 | 可解释性依赖暴露多少内部证据 | 组件实验出现明确复用需求时增加内部 Seam |
| 方法声明与实例输出双重验证 | 支持某能力不保证每条都正确输出 | 需要预检和运行后校验 | 不应取消，可优化校验成本 |
| 两种执行 profile | 文件请求和在线 session 生命周期真实不同 | 比单一 transcribe 多一份契约 | 主动交互/设备控制等改变输入时增加新 profile |
| 共用 envelope，不统一 tensor | 可追溯性稳定，内部表示变化快 | 某些扩展类型只能专用消费 | 两个以上独立消费者需要共享语义时升级 |
| 事件用 snapshot 语义 | 修订、合并、删段可确定回放 | naive 存整段快照成本高 | 存储压缩，不改变公共回放语义 |
| 输入和金标隔离 | 公平性不能靠模型“自觉不看” | worker 访问配置要认真做 | 本地可信/严格环境可有不同保证级别 |
| 数据 View 与方法步骤分开 | 同一变换放不同位置代表不同实验 | 用户需显式选择变换角色 | 提供预设简化配置，不合并语义 |
| 分开的预测与评分缓存 | 调 scorer 不应重推理；状态变化必须重推理 | 精确依赖身份较复杂 | 先保守禁用无法证明安全的缓存 |
| 成功率与全名单交付分数共报 | 防困难样本失败被删而得高分 | 单一排行榜更难简化 | 可做业务门槛，但不丢失败事实 |
| 不先绑定现有 harness | 避免继承隐式数据/评分/时间假设 | 自己维护小契约层 | 四类契约实验通过后可选一后端，不改变领域契约 |

## 5. Locality 审查：发生变化应该改哪里

| 未来变化 | 应改 | 不应改 | 若越界，先检查 |
|---|---|---|---|
| 新增一个返回全文的云服务 | 方法 adapter、参数 schema、注册 | 数据、普通 CER scorer、报告 | 是否把 provider 响应直接泄到 report |
| 新增一种 HF/私有数据格式 | source adapter、映射验证 | 方法 runner、模型 adapters | 是否 method 直接读取 dataset 特有列 |
| 新加日语/韩语评分规范 | ScoreSpec/语言规则与样例 | 模型输出、运行记录 | 是否把 normalizer 放进方法输出解析 |
| 新加 LLM 纠错 | recipe/plugin、其配置和 trace | 全局调度、DatasetRecord 字段 | 是否只有后处理 string hook 但实际需要音频/候选 |
| 新加多候选图消费者 | 扩展 schema/validator 和消费 adapter | 所有基础模型 | 是否把某图结构提升得过早 |
| 新加模型热词参数 | 该方法参数校验与允许资源绑定 | 通用 boost 标量及其他 adapter | 是否错误承诺相同热词数值语义 |
| 修正 reference | 对应层 revision 与 ScoreRun | 原预测及运行时间 | reference 是否不当地参与 inference key |
| 修改会话历史 | 输入资源及受影响状态后继 | 不相关 episode | cache 是否忽略前置状态 |
| 加入视觉信息 | Media/同步 adapter、独立输入协议、组合方法 | 音频模型/文字 scorer | 是否以扩展媒体为借口混同输入公平性 |
| 加入新的 endpoint 指标 | ScoreSpec/metric adapter，检查 trace 需求 | provider 算法 | 可观察 trace 是否不足，要诚实标不可测 |

期望不是“所有新方法只写一个文件”，而是需要改动的知识集中在正确位置。一个全新指标合理地需要 scorer；一个新来源合理地需要数据 adapter。比文件数更重要的是核心是否开始出现 `if model_name` / `if dataset_name`。

## 6. 本次自审发现的易错点与防线

### 6.1 可比性 key 不能等于完整实验 hash

完整实验身份必须包含方法；但比较两个方法时方法恰恰要不同。因此分别保留 RunIdentity/MethodIdentity 与 ComparisonKey。系统比较允许在相同信息权限内有不同检索选择；受控消融再锁实际资源。否则“严格可复现”反而让不同方法永远无法比较。

### 6.2 输出能力与评分要求不能互相偷偷修补

对齐器可以补词时间，但这是额外方法步骤。scorer 若自动调用它，会引入未记录成本和新的错误来源；且不能用对齐得到的时间评价原模型的原生时间精度。因此 ScoreRun 必须离线读取已固定产物，不得顺便执行模型。

### 6.3 数据覆盖要在评分前成立

一个数据集宣称有 speaker 标注，不代表每条每秒都完整。ReferenceBundle 必须带覆盖和评分区；无预测时间时，不能在局部 gold 中自由挑字符串匹配。资格检查在所有模型运行前冻结，不在看到失败后删困难样本。

### 6.4 统计单位不等于方法处理单位

方法可以处理窗口、通道或 utterance；主要指标的归并单位由协议固定，bootstrap 独立组由来源决定。不能窗口数翻十倍就把置信区间缩成十倍样本量；不能 speaker 排序变化导致“纯标签方法”的词序也被评分器改掉。

### 6.5 时间可观察性有上限

不能观测云端排队就记录 unknown。可以测客户端实际看到的结果，不把客户端时钟和 provider 时钟直接相减。假定网络时延已知、用录音中 end time 当输出时刻，都会制造漂亮且不可证实的性能。

### 6.6 缓存很容易把状态错误掩盖成速度收益

质量重评分可以复用结果；实时性能需重跑。隐藏外部状态无法冻结时禁用预测缓存。只存 seed 而不固定媒体内容/算子版本也不能保证加噪数据相同。缓存以依赖事实为依据，不以步骤名称为依据。

### 6.7 `NA` 不能成为免责通道

方法预先没承诺的可选维度不可测，正确报告 NA；必需能力缺失则预检不能进入该任务。已经承诺的必需字段在运行后丢失是失败，不能临时改成可选指标。报告必须展示两者差别。

## 7. 契约验收清单

这些是未来实现的验收规格，目前状态全部为“未执行”。低风险文档变更不需要伪装成已通过单元测试。

| ID | 场景 | 可观察的通过条件 |
|---|---|---|
| D01 | 同一条录音由本地 CSV 和 HF adapter 读取 | 源信息不同但媒体/参考语义等价，结果可追溯 |
| D02 | 无参考文本 vs 确认静音 | 前者资格不满足，后者合法空参考且插入被统计 |
| D03 | 四麦录音进入单麦和多麦任务 | 原轨不被覆盖，输入权限和 cohort 不混淆 |
| D04 | padding 重叠切片 | 源时间映射正确，同一主评分词不重复统计 |
| D05 | 整句 gold 随机切半且无时间标注 | 不能自动宣称拥有半句 gold |
| D06 | episode 打乱或未来上下文提前注入 | 顺序/availability 检查拒绝 |
| D07 | 移动路径、改 gold、改输入历史三种改动 | 分别复用、只重评分、重跑状态后继 |
| D08 | train/test 包含同父录音不同片段 | 分组审计发现跨集；不因文件名不同放过 |
| M01 | 新增第二种 whole-case 模型 | 普通数据/运行/文字 scorer 不需模型名分支 |
| M02 | 联合 SATS 与级联方法 | 两者合法输出相同目标语义，无伪阶段或伪词时间 |
| M03 | API 无 N-best 接 N-best recipe | 预检拒绝，不复制候选、不暗换算法 |
| M04 | ECLM 回调 candidate scoring | 输入/版本绑定，调用和费用记入总账；score 语义声明 |
| M05 | 纯 Attribution 修改 | Transcript 不变；固定词序文字分数严格不变 |
| M06 | 插词后沿用旧词 alignment | validator 检测 revision/跨度失配 |
| M07 | 异常格式/输出截断 | 原响应保留，状态/覆盖明确，不冒充完整成功 |
| R01 | 多次 partial 同 scope | replay 得到替换后的终态，不重复追加 |
| R02 | 文字定稿后 speaker 回改 | 文字和归属稳定分别记录 |
| R03 | 提交不可变内容之后改字 | 记录承诺违约，保持真实 trace |
| R04 | 模型试图读取未来整文件 | 严格 profile 不提供该访问；可信模式明确保证较弱 |
| R05 | 输入结束后 provider 晚到结果 | 按 drain 截止区分 on-time/late，不能回填早分数 |
| R06 | 断线后无 checkpoint | 新 attempt 或重放，原失败/重复代价保留 |
| R07 | 开放到达负载下处理变慢 | 队列/超时/拒绝暴露，发生器不能自动悄悄降负载 |
| S01 | 一条成功、一条超时 | 全名单交付分母固定；成功子集另报 |
| S02 | 不同 normalizer/reference 的结果 | 拒绝直接排名，能从旧产物重新评分 |
| S03 | 无词时间却请求词延迟 | 不产生伪词时刻，报告资格不足 |
| S04 | 一半词快速稳定，另一半从未输出 | 延迟分位数旁同时显示未覆盖率 |
| S05 | 多参考交给不支持的 scorer | 编译拒绝或使用独立预定仲裁协议 |
| S06 | 普通 WER 百分比逐条平均 | 与累积 S/D/I/N 的协议期望不符，验收失败 |
| C01 | normalizer 变化后重评分 | 无新模型调用，旧结果不覆盖 |
| C02 | 会话第二轮状态变化 | 只复用可证明依赖不变的后继；其他需重算 |
| C03 | 重试成功但前一次可能计费 | 两次 attempt 与费用不确定性均保存 |
| C04 | 用质量缓存跑“延迟测试” | 明确非新性能测量，不能进入性能 cohort |
| C05 | 并行融合三个识别器 | 墙钟和累计计算/调用量分别记录 |

真实模型的烟雾实验应覆盖一个本地模型、一个 API、一个联合输出方法、一个有修订的流式方法；不是测更多相似模型来替代以上契约检查。真实运行前需要可用数据、模型和环境；本次没有假设它们已经准备好。

## 8. 两个手工计算例子

### 失败不能在分母消失

假设 A 的参考 4 词，成功输出 1 个错误；B 的参考 6 词，到截止仍超时无输出。按本文默认交付口径，B 作为空假设有 6 个删除，总错误率是 `(1+6)/(4+6)=70%`，交付率为 1/2。成功子集错误率是 `1/4=25%`，只能用于诊断。不能只发布 25% 并称整个测试完成。

如果 B 有符合预定合法投影的部分结果，评分截止 snapshot 的真实内容，而不是一律丢弃。失败状态仍保留。这一规则在运行前冻结。

### 只改金标与改历史的影响不同

同一两轮会话，第二轮有一个人工参考错字。修正只用于打分的 gold，不改变 InputView，因此原推理可以复用，建立新 ScoreRun。

若修正的是第二轮输入里实际提供的人工历史，则 MethodSpec 相同也必须重跑第二轮；若第二轮更新记忆，还应重跑其有状态后继。这不是“缓存策略偏好”，而是输入函数关系已经改变。

## 9. 最小实现应怎样保持轻量

第一阶段只需要：manifest 与 schema 文档、source/method/score 三类 adapter、有限执行 profile、文件型不可变产物、单机 runner 和报告。目录组织可以是逻辑上的 contracts/data/protocols/execution/artifacts/scoring/methods，不要求先变成多个包。

首版不做：自动发现任意第三方插件、复杂类继承、通用图编辑器、微服务、全局事件总线、跨集群缓存、在线实验平台、每种内部 tensor 的公共格式。日志和 schema 也只保留有明确评测用途的字段。

可配置不等于任意可执行 hook。普通模型接入填写参数 schema 和合法输出映射；研究方法可封装整个实现。真正有两个使用者的复用才提取 Module，避免为未来所有可能性提前建立接口。

## 10. 尚未验证的假设与下一步优先级

| 不确定性 | 为什么会影响设计/实施 | 最小验证方式 |
|---|---|---|
| 具体 API 的修订/停止/错误信息是否足够 | 决定可测指标，不保证每个后端都能满足 | 捕获实际响应样例，校验 provider adapter 映射 |
| 模型依赖能否隔离且代价合理 | 决定 worker/后端选型 | 两个依赖冲突模型分别启动，再共用同一协议 |
| candidate scorer 的语义/成本 | 决定 ECLM 组合是否真实成立 | 固定 checkpoint 验证指定候选评分，不以 confidence 替代 |
| 多轨/视频同步的实际可得性 | 决定输入是否能满足方法前提 | 对目标数据做同步/缺轨审核 |
| trace 的存储量和回放开销 | snapshot 语义若直存长稿会膨胀 | 合成修订 trace 做等价压缩与回放，不需先跑大模型 |
| 词稳定配对在重复词/多说话人中是否稳定 | 影响延迟定义的实现一致性 | 固定手工 trace/gold 样例，明确 tie-breaking 后交叉核算 |
| scorer 格式转换有无丢失 | 可能静默改变时间和 speaker | 导入导出 round-trip，核对 source-time 和文本单位 |

优先先验证“相同协议下两个不同后端能否留下可重评分、失败不丢样本的结果”，再加组合/会议/流式。这样每增加一层复杂性，都有已有契约守住抽象；不是等做完所有模型才发现基础数据身份错误。

## 11. 本次交付与完成判定

已完成：总体架构、独立数据抽象、24 类机制的适配检查、8 类方法的一手证据、四轮反例审查、变更位置表、33 项未来验收规格，以及明确的不可支持/需要新 profile 的情况。

没有完成且不在本次“只设计不写代码”范围：实现 adapters、下载数据/权重、跑分、性能验证、第三方 backend 选型的实机证据。设计可进入实现评审；不得描述为已运行成功的 framework。
